// import { selectDetailUserPageDomain } from '../selectors';

describe('selectDetailUserPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
