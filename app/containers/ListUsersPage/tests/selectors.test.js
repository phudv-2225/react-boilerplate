// import { selectListUsersPageDomain } from '../selectors';

describe('selectListUsersPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
